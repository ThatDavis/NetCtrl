# Specification — NetCtrl

> Last updated: 2026-05-28
> Generated by `/dev:scaffold-existing`

## Overview

NetCtrl is a terminal TUI for logging amateur radio net check-ins. Operators manage multiple nets (each with frequency, offset, PL tone, and club info), create dated sessions for each net occurrence, and log check-ins with automatic UTC timestamps. The app persists all data locally and supports theming, callsign autocomplete, and FCC lookup.

## Milestone 1 — Core TUI & Logging

### Features

#### Feature: Net Management
**Status:** Complete
**Description:** Create, edit, and delete nets. Each net stores name, club, frequency, offset, PL tone, digital flag, mode, and mode notes.
**Acceptance Criteria:**
- [x] `n` in Nets focus opens add-net dialog
- [x] `e` edits selected net
- [x] `d` deletes net with confirmation
- [x] All fields persist to disk

#### Feature: Session Logging
**Status:** Complete
**Description:** Each net holds multiple sessions. Sessions have a date and net_time. Legacy flat check-ins are auto-migrated into a single session on load.
**Acceptance Criteria:**
- [x] `n` in Sessions/Log focus creates a session for today
- [x] Sessions display in chronological list
- [x] `d` deletes session with confirmation
- [x] `e` edits session date/time

#### Feature: Check-in Logging
**Status:** Complete
**Description:** Log callsign, name, nickname, and remarks per check-in. Automatic UTC timestamp.
**Acceptance Criteria:**
- [x] `c` opens add-checkin dialog
- [x] Check-ins append to active session
- [x] Timestamps formatted as `HH:MMz`

#### Feature: Callsign Autocomplete & FCC Lookup
**Status:** Complete
**Description:** Suggest matching callsigns from history as you type. Background query to callook.info fills operator name.
**Acceptance Criteria:**
- [x] Dropdown of up to 8 matches appears while typing callsign
- [x] `↑`/`↓` cycles suggestions; `Tab`/`Enter` applies
- [x] Background HTTP request to callook.info on field blur
- [x] Name field auto-populated when result arrives

#### Feature: Theming
**Status:** Complete
**Description:** 8 built-in Base16 themes plus custom TOML themes in `~/.config/netcontrol/themes/`.
**Acceptance Criteria:**
- [x] `t` opens theme picker
- [x] Selected theme persists across launches
- [x] Custom themes appear in picker

#### Feature: Export
**Status:** Complete
**Description:** Export any session to a formatted `.txt` file.
**Acceptance Criteria:**
- [x] `x` opens export dialog with filename input
- [x] File saved to `~/`
- [x] Format includes net info, session date/time, and check-in table

---

## Milestone 2 — Scheduling & Automation

### Features

#### Feature: Scheduled Net Sessions
**Status:** Complete
**Description:** When creating a session, present a modal with two options: "Create Now" (default) or "Schedule for Later". Scheduled sessions store a target datetime and persist to disk. When the app is within 5 minutes of a scheduled session's start time, an alert overlay appears in the sessions pane showing a countdown timer with diagonal striped background. When the countdown reaches zero, the session auto-opens and the add-checkin dialog appears.
**Acceptance Criteria:**
- [x] Pressing `n` in Sessions/Log focus opens a session-creation modal instead of immediately creating a session
- [x] Modal has "Create Now" as the default/primary option
- [x] Modal has "Schedule for Later" as a secondary option with datetime input
- [x] Scheduled sessions persist in `~/.netcontrol_data.json`
- [x] Scheduled sessions display in the session list with an indicator (e.g., "SCHEDULED")
- [x] When current time is within 5 minutes of a scheduled session start, an alert overlay appears
- [x] Overlay shows "ALERT" header, large countdown timer (MM:SS), and "NET STARTING" footer
- [x] Overlay has diagonal stripe pattern background using the active theme's accent colours
- [x] Countdown updates every second
- [x] When countdown reaches zero, the scheduled session is opened (entered) automatically
- [x] When countdown reaches zero, the add-checkin dialog (`CiDlg`) opens automatically
- [x] If multiple sessions are scheduled within the same 5-minute window, alert for the nearest one

## Non-Functional Requirements

- Single binary, no runtime dependencies beyond libc
- Startup time < 500ms
- JSON data forward-compatible via `#[serde(default)]`

## Open Questions

- [ ] Should scheduled sessions support recurrence (weekly, etc.)? — Owner: ThatDavis, Due: TBD
